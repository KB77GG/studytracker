/**
 * Aliyun oral evaluation SDK bridge.
 *
 * Replace this stub with the official mini-program SDK integration.
 * Expected return shape:
 *   { ok: true, data?: object, recordIds?: string[] }
 */
async function evaluate() {
  return {
    ok: false,
    error: 'oral_sdk_not_integrated'
  }
}

module.exports = {
  evaluate
}
